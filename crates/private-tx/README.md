# miden-node-private-tx

Node-local proof-of-concept helpers for private validator mode.

The crate owns envelope types, signed submission-key discovery helpers,
associated-data construction, TEE traits, threshold adapter traits, audit
coordination traits, and test adapters while the design is still moving.
Stable protocol types can move to `miden-protocol` later.
